<%= form_for @post, html: { multipart: true } do |p| %>
  <div class="form-group">
    <%= p.label :Titulo, for: "title"  %></br>
    <%= p.text_field :title, id: "title", class:"form-control", placeholder:"Aquí debe de ir un gran titulo!" %>
  </div>

  <div class="form-group">
    <%= p.label :Portada, placeholder: "Carga la portada" %>
    <%= p.file_field :avatar %>
  </div>

  <div class="form-group">
    <%= p.label :Articulo %></br>
    <%= p.cktext_area :body, col: 40 %>
  </div>

  <p>
    <%= p.submit "Guardar post", class:"btn btn-default"%>
  </p>
<% end %>
